name: Hadolint
on:
  push:
  pull_request:
  schedule:
    - cron: '0 20 * * *'

jobs:
  build:
    name: Hadolint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Hadolint
        uses: hadolint/hadolint-action@master
